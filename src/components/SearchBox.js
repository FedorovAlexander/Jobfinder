import React from 'react';

const SearchBox = () => {
  return (
    <div>
      <input
        className="input"
        type='search'
        placeholder='search vacancies'
       />
    </div>
  );
}

export default SearchBox;
